document.addEventListener("DOMContentLaoded", function() {
    const divElements = document.getElementsByClassName('item');
    const elementsCount = divElements.length;
    console.log('Nombre de divs avec la classe "item":', elementsCount);

});